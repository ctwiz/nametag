<div class="followers">
  <h2>Followers</h2>
  <% @followers.each do |f| %>  
    <div class="follower">
      <div class="name">
        <%=link_to user_path(f.follower.id) do %>
        <%=f.follower.handle%>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
